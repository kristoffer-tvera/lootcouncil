@{
    Layout = "_Layout";
    var region = Context.Request.Cookies["region"];
    if (string.IsNullOrWhiteSpace(region))
    {
        Context.Response.Cookies.Append("region", Constants.Regions.First());
    }
}
